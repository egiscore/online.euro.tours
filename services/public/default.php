<?php
 use Phalcon\Di\FactoryDefault; use Phalcon\Mvc\Application as App; ini_set('display_errors', 1); error_reporting(E_ALL); set_time_limit(0); defined('BASE_PATH') || define('BASE_PATH', dirname(__DIR__)); defined('ROOT_DIR') || define('ROOT_DIR', dirname(dirname(__DIR__))); defined('APP_PATH') || define('APP_PATH', BASE_PATH . '/app'); $di = new FactoryDefault(); include_once ROOT_DIR . '/properties.php'; include APP_PATH . '/config/loader.php'; include APP_PATH . '/config/services.php'; include APP_PATH . '/config/router.php'; include ROOT_DIR . '/routes.php'; include_once ROOT_DIR . '/includes/common.php'; include_once ROOT_DIR . '/includes/db.php'; try { $application = new App($di); echo $application->handle()->getContent(); } catch (\Exception $e) { echo $e->getMessage() . '<br>'; echo '<pre>' . $e->getTraceAsString() . '</pre>'; } 